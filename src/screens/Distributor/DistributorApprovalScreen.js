import React from 'react';
import ApprovalDashboardScreen from '../Common/ApprovalDashboardScreen';

// Distributor approves FSEs that selected them as their parent.
const DistributorApprovalScreen = () => (
  <ApprovalDashboardScreen title="FSE Approvals" />
);

export default DistributorApprovalScreen;
